_FAT_LTO_OBJECTS="-flto -ffat-lto-objects"
CFLAGS="${CFLAGS} ${_FAT_LTO_OBJECTS}"
CXXFLAGS="${CXXFLAGS} ${_FAT_LTO_OBJECTS}"
FCFLAGS="${FCFLAGS} ${_FAT_LTO_OBJECTS}"
FFLAGS="${FFLAGS} ${_FAT_LTO_OBJECTS}"
